version: '3.4'
services:
  nexus:
    image: sonatype/nexus3
#    ports:
#      - 8443:8443
    networks:
      - proxy
    volumes:
      - nexus-data:/nexus-data
#    secrets:
#      - keystore.jks
    deploy:
      replicas: 1
      labels:
        - com.df.notify=true
        - com.df.distribute=true
        - com.df.serviceDomain.1=nexus.swarm.usc.edu
        - com.df.port.1=8081
        - com.df.httpsOnly.1=true
        - com.df.xForwardedProto.1=true
        - com.df.xForwardedProto=true
        - com.df.serviceDomain.2=registry.swarm.usc.edu
        - com.df.port.2=8445
        - com.df.httpsOnly.2=true
        - com.df.serviceDomain.3=dtr.swarm.usc.edu
        - com.df.port.3=8442
        - com.df.httpsOnly.3=true
      update_config:
        parallelism: 1
        delay: 10s
      restart_policy:
        condition: on-failure
#    environment:
#      - INSTALL4J_ADD_VM_PARAMS=-DJETTY_PASSWORD=${USC_NEXUS_KEYSTORE_PASSWORD}
networks:
  proxy:
    external:
      name: proxy
volumes:
  nexus-data:
    external: true
# secrets:
#  keystore.jks:
#    external: true
